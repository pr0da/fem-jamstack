import React from "react"
import { RouteComponentProps } from "@reach/router"

const RouteBase: React.FC<RouteComponentProps> = () => {
  return <h1>All your base are belong to us!</h1>
}

export default RouteBase
